export const PhotosFooter = () => {
  return (
    <section className="text-center">
      <button className="border-2 border-accent text-accent hover:bg-accent hover:text-white hover:shadow-[0_0_20px_rgba(0,128,255,0.3)] transition-all duration-300 font-semibold px-8 py-2 rounded-md text-base bg-transparent">
        Lihat Galeri Lengkap
      </button>
    </section>
  );
};
